<views:MvxWindowsPage
    x:Class="AppUwp.Views.MainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AppUwp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:views="using:MvvmCross.Uwp.Views"
    mc:Ignorable="d">
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel Margin="20" HorizontalAlignment="Center" Width="500" Height="250" VerticalAlignment="Center">
            
            <Button Content="Database Management" Command="{Binding DatabaseMvxCommand}" FontSize="25" HorizontalAlignment="Stretch"></Button>
            <Button Content="Add Family" Command="{Binding AddFamilyCommand}" FontSize="25" HorizontalAlignment="Stretch"></Button>
            <Button Content="Search Family" Command="{Binding SearchMvxCommand}" FontSize="25" HorizontalAlignment="Stretch"></Button>
        </StackPanel>
    </Grid>
</views:MvxWindowsPage>

